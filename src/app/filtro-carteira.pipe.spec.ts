import { FiltroCarteiraPipe } from './filtro-carteira.pipe';

describe('FiltroCarteiraPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCarteiraPipe();
    expect(pipe).toBeTruthy();
  });
});
